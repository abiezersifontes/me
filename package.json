{
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "export": "next export",
    "deploy": "npm run build && gh-pages -d out"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "next": "latest",
    "react": "18.2.0",
    "react-bootstrap": "^2.10.5",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
